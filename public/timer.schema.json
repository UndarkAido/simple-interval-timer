{
  "$id": "https://example.com/timer.schema.json",
  "title": "Interval Timer Config",
  "description": "A configuration for an interval timer",
  "type": "object",
  "properties": {
    "macros": {
      "description": "Custom actions, defined by a name and a sequence",
      "type": "object",
      "patternProperties": {
        "\\w+": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "sequence": {
      "description": "A series of actions, the interval execution sequence of the timer",
      "type": "array",
      "items": {"type": "string"},
      "minItems": 1
    }
  },
  "required": ["sequence"],
  "additionalProperties": false
}

